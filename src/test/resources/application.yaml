service:
  name: "pulse-backend"
  url: "https://pulse.example.test"
  timeout: 10
feature:
  enabled: false
db:
  list:
    - events
  events:
    url: "jdbc:postgresql://localhost:5432/pulse_events"
    dialect: "POSTGRES"
    username: ""
    password: ""
    pool:
      maxSize: 10
      minIdle: 0
kafka:
  bootstrap:
    servers: "localhost:9092"
  consumer:
    auto:
      offset:
        reset: "latest"
    poll:
      ms: 500
  topics:
    example:
      events: "pulse.example.events"
      results: "pulse.example.results"
